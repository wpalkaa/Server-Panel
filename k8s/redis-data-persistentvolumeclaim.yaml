apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: server-panel
  labels:
    io.kompose.service: redis-data
  name: redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
